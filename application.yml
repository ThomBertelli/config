eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
#  endpoint:
#    health:
#      show-details: always
info:
  author: Thomas B. B. Mendes
  mail: bertellimendes@gmail.com
  cel: 313313131
server:
  port: ${PORT:9999}
message: Mensagem generica
